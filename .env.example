# Turso / LibSQL — production (Netlify)
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Optionnel: chemin local pour le dev (si pas de Turso)
# LOCAL_DB_PATH=./data/todo.db
